# Frontend-only local run (optional).
# For `npm run dev` set where `/api/*` should proxy in dev server.
VITE_DEV_PROXY_TARGET=http://localhost:8000

# Optional direct API URL bypassing dev proxy.
# VITE_API_URL=http://localhost:8000
