# Frontend-only local run (optional)
# Current Vite dev config proxies /api to backend service in Docker.
# If you add direct API URL usage in frontend env, place it here.
VITE_API_URL=http://localhost:8000
